# Browser-restricted Google Maps JavaScript API key.
# For local preview, allow http://localhost:* and http://127.0.0.1:* as HTTP referrers.
# For production, also allow the deployed site domain.
GOOGLE_MAPS_API_KEY=replace_me
