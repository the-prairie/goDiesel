# macOS / editor noise
.DS_Store
*.swp
*.swo

# Python local state
__pycache__/
*.py[cod]
.venv/
venv/

# Local secrets and private config
.env
.env.*
!.env.example

# Local caches generated by admin/build scripts
.geo_cache.json
.geocode_buckets.json
.rebuild.log

# Generated build output.
/index.html
/cards/
/photos/
/dist/
/app/dist/
/app/artifacts/

# Cloudflare/Wrangler local account cache
.wrangler/

# Temporary files
tmp/
*.tmp

# Node dependencies (app/ has its own ignore; this covers the repository root)
node_modules/

# Published route media is product content and is tracked. Uncomment only if
# the photo library moves out of the repository.
# /app/public/media/

# Local experiments and unrelated build output, not part of goDiesel
/lottie-player/
/target/
